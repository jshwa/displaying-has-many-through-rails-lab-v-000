<h1>Patients#index</h1>
<ul>
  <% @patients.each do |patient| %>
  <li>Name: <%= link_to patient.name, patient_path(patient.name) %> Number of Appointments: <%= patient.appointments.count %></li>
</ul>
